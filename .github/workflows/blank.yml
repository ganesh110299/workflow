
name: first workflow
on: push
#   pull_request:
#     branches:    
#       - main
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: print hello
      run: |

          ls
          cd /home/runner/work/workflow/workflow
          git config --global user.email "g.dubbaka@dxc.com" 
          git config --global user.name "ganesh110299"
           git clone https://github.com/ganesh110299/workflow.git
          git checkout master
          echo $hi >> random.txt
          git add -A
          git commit -m "random file"
          git push --set-upstream origin main


